{% extends 'base.html.twig' %}

{% block title %}Chocolate{% endblock %}

{% block body %}
    <h1>Chocolate</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chocolate.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ chocolate.name }}</td>
            </tr>
            <tr>
                <th>Brand</th>
                <td>{{ chocolate.brand }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ chocolate.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chocolate_index') }}">back to list</a>

    <a href="{{ path('chocolate_edit', {'id': chocolate.id}) }}">edit</a>

    {{ include('chocolate/_delete_form.html.twig') }}
{% endblock %}
